{
    "name": "Miniconda (Python 3)",
    "build": {
        "context": "..",
        "dockerfile": "Dockerfile"
    },
    "postCreateCommand": "(mamba shell hook --shell zsh && echo mamba activate sage) >> ~/.zshrc",
    "remoteUser": "root"
}